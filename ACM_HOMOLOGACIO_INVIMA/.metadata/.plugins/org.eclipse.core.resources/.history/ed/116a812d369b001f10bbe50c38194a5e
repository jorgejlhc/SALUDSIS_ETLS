package routines;

import java.util.UUID;

/*
 Método que genera el GUID del archivo de salida log de errores CT
*/
public class GenerarUuid {

    public static String createUUID() {
    	String newId = UUID.randomUUID().toString();
    	return(newId);
    }
}
